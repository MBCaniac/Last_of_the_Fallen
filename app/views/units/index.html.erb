<h1>Last of the Fallen</h1>

<ul>
    <% @units.each do |unit| %>
        <li>
            <%= link_to unit.name, unit %>
        </li>
    <% end %>
</ul>

<%= link_to "New Unit", new_unit_path %>
<%= link_to "New Location", new_location_path %>
<%= link_to "Edit Location", edit_location_path(@location, :id=>1) %>

